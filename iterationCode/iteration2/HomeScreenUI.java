public class HomeScreenUI {
	
	HomeScreenUI() {
		// Display a new GUI window that contains a welcome text
		// and two buttons. One for Register and one for LogIn.

		// For which ever button is called call the relevant 
		// method in the HomeScreen class to deal with it.
	}
}