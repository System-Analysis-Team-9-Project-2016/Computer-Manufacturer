public class Register {
	
	Register() {
		// Create a new object of type RegisterUI (GUI screen)
		// RegisterUI createNewRegisterUI = new RegisterUI();
	}

	goBackButtonClicked() {
		// Create a new object of type HomeScreen.java
		// HomeScreen createNewHomeScreen = new HomeScreen();
	}

	processRegisterButtonClicked() {
		// Read in the details the cusotmer has entered in the text fields on the UI.
		// Check to see if those details are already present in the customerList.txt file
		// which contains the list of registered Customers. 

		// If they are already present, keep asking them to input details until they either 
		// click 'Go Back' button or they input unused details.

		// If the details are usable, append them to the end of the customerLIst.txt file and
		// create a new Customer object.
		// e.g. String customerName , String customerEmail, String customerPassword, String creditCardNumber
		
		// Call object of type ProductList and pass in the Customer object created.
		// ProductList createNewProductList = new ProductList(currentCustomer);
	}
}