public class AddNewProduct {
	
	AddNewProduct(Admin currentAdmin) {
		// Create new object of type AddNewProductUI
		// AddNewProductUI createNewAddNewProductUI = new AddNewProductUI();


	}

	processGoBackButtonCLicked(Admin currentAdmin) {
		// Create a new object of type ProductList
		// ProductList createNewProductList = new ProductList(currentAdmin);
	}

	processCreateProductButtonClicked(Admin currentAdmin) {
		// Check if product is already listed in productList.txt
		// If it is, make the Admin input correct details.
		// If they are valid details for a new product, append them to the end of the productList.txt file.

		// Create a new object of type Product List
		// ProductList createNewProductList = new ProductList(currentAdmin);
	}
}