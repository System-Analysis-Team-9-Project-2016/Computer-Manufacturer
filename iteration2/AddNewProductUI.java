public class AddNewProductUI {
	 AddNewProductUI() {
	 	// Display text fields required to create a new object
		// e.g. int productId, String productName, int stock, double unitCost, boolean isActive, boolean isProductDiscount

		// Also displayed are two buttons 'Create Product' and 'Go Back' that
		// when clicked called their methods in AddNewProduct.java
	 }
}